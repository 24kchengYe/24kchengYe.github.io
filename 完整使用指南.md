
# 📚 个人学术主页完整使用指南

本指南全面介绍个人学术主页系统的所有功能,包括AI驱动的图片处理、JSON配置文件管理和内容更新操作。

---

## 📋 目录

### 第一部分:图片自动处理
1. [功能概述](#功能概述)
2. [环境配置](#环境配置)
3. [论文封面自动提取](#论文封面自动提取)
4. [头像智能裁剪](#头像智能裁剪)
5. [图片处理故障排除](#图片处理故障排除)
6. [高级配置](#高级配置)

### 第二部分:内容更新管理
7. [更新方式概览](#更新方式概览)
8. [配置文件详解](#配置文件详解)
9. [论文数据管理](#论文数据管理)
10. [数据集管理](#数据集管理)
11. [奖项管理](#奖项管理)
12. [学术活动管理](#学术活动管理)
13. [News管理](#news管理)
14. [AI格式化助手](#ai格式化助手)

### 第三部分:常见场景
15. [日常使用场景示例](#日常使用场景示例)
16. [常见问题解答](#常见问题解答)

---

# 第一部分:图片自动处理

## 功能概述

### 🤖 AI驱动的自动化

本系统使用OpenAI GPT-4 Vision API实现:
1. **PDF论文封面提取** - 自动识别论文中最具代表性的图表
2. **头像智能裁剪** - 自动检测人脸并裁剪为正方形
3. **论文元数据提取** - AI自动识别标题、作者、期刊等信息

### 🔄 三重回退策略

系统设计了三重回退机制,确保即使AI服务不可用,图片处理仍能完成:

**论文封面提取**:
1. **AI识别** (首选) - GPT-4 Vision识别框架图/核心图表
2. **首页截图** (回退1) - 使用PDF首页完整截图
3. **手动裁剪** (回退2) - 提示用户手动处理

**头像裁剪**:
1. **AI人脸检测** (首选) - GPT-4 Vision识别人脸位置
2. **OpenCV本地检测** (回退1) - 使用Haar级联分类器
3. **中心裁剪** (回退2) - 裁剪图片中心区域

---

## 环境配置

### 步骤1: 安装Python依赖

```bash
# 进入项目根目录
cd 046个人主页

# 安装所有依赖
pip install -r scripts/requirements.txt
```

**依赖包含**:
```
PyMuPDF>=1.23.0          # PDF处理
Pillow>=10.0.0           # 图片处理
python-dotenv>=1.0.0     # 环境变量管理
openai>=1.0.0            # OpenAI API客户端
anthropic>=0.8.0         # Claude API (可选)
opencv-python>=4.8.0     # 本地人脸检测 (可选)
```

### 步骤2: 获取OpenAI API密钥

#### 方法1: 官方OpenAI账户

1. 访问 [OpenAI Platform](https://platform.openai.com/)
2. 注册并登录账户
3. 进入 **API Keys** 页面
4. 点击 **Create new secret key**
5. 复制生成的密钥(格式:`sk-...`)

**费用参考** (2025年1月):
- GPT-4 Vision: $0.01/image (1024×1024以下)
- GPT-4 Turbo: $0.01/1K tokens
- 预计成本:处理1篇论文 ≈ $0.02-0.05

#### 方法2: 第三方中转服务 (国内推荐)

如果无法访问OpenAI官网,可使用中转服务:
- [OpenAI-SB](https://openai-sb.com/)
- [API2D](https://api2d.com/)
- [CloseAI](https://console.closeai.biz/)

**优势**:
- 国内可直接访问
- 支持支付宝/微信支付
- 通常更稳定

### 步骤3: 配置 .env 文件

```bash
# 复制配置模板
cp .env.example .env

# 编辑 .env 文件
# Windows: notepad .env
# Mac/Linux: nano .env
```

**配置内容**:
```bash
# ========== OpenAI API配置 ==========
# 您的OpenAI API密钥(必填)
OPENAI_API_KEY=sk-your-actual-key-here

# API端点(国内用户需修改)
# 官方: https://api.openai.com/v1
# 中转示例: https://api.openai-sb.com/v1
OPENAI_BASE_URL=https://api.openai.com/v1

# 图片识别模型(默认:gpt-4-vision-preview)
OPENAI_MODEL=gpt-4-vision-preview

# 文本处理模型(用于元数据提取)
CONTENT_FORMAT_MODEL=gpt-4-turbo-preview

# ========== 图片处理参数 ==========
# 头像尺寸(正方形,默认400)
AVATAR_SIZE=400

# 论文封面尺寸(4:3比例)
PAPER_COVER_WIDTH=400
PAPER_COVER_HEIGHT=300

# ========== 本地人脸检测 (可选) ==========
# 是否启用OpenCV本地检测作为备选
USE_LOCAL_FACE_DETECTION=true
```

**⚠️ 安全提示**:
- `.env` 文件已添加到 `.gitignore`,不会被Git跟踪
- 切勿将API密钥提交到公开仓库
- 部署到GitHub Pages时无需 `.env`(仅本地使用)

### 步骤4: 验证配置

```bash
# 运行测试命令
python scripts/image_processor.py --guide

# 如果配置正确,会显示使用指南
# 如果API密钥无效,会提示错误
```

---

## 论文封面自动提取

### 🎯 功能说明

**输入**: PDF论文文件
**输出**:
- 400×300像素封面图 (保存至 `images/papers/`)
- PDF副本 (保存至 `pdfs/` 供网站下载)
- 完整元数据 (自动添加到 `data/publications.json`)
- News条目 (自动添加到 `data/news.json`)

**处理时间**: 约30-60秒/篇(取决于网络)

### 📝 使用步骤

#### 方式1: 处理单个PDF

```bash
# 1. 将PDF放入指定文件夹
cp your_paper.pdf images/raw-papers/

# 2. 运行处理脚本
python scripts/image_processor.py --papers

# 3. 等待AI处理...
# [OK] 正在提取PDF首页...
# [OK] 使用AI识别代表性图表...
# [OK] 裁剪并保存封面: images/papers/your_paper.png
# [OK] PDF已复制至下载目录: pdfs/your_paper.pdf
# [OK] 提取论文元数据...
# [OK] 格式化为APA引用...
# [OK] 已添加到 data/publications.json
# [OK] 已生成News条目
```

#### 方式2: 批量处理

```bash
# 1. 将所有PDF放入文件夹
cp paper1.pdf paper2.pdf paper3.pdf images/raw-papers/

# 2. 批量处理
python scripts/image_processor.py --papers

# 系统会逐个处理所有PDF,自动跳过已处理的文件
```

#### 方式3: 仅提取封面(不提取元数据)

```bash
# 如果只需要封面图,不需要元数据
python scripts/image_processor.py --papers --no-metadata
```

### 🔍 处理流程详解

**第1步: PDF渲染**
```python
# 使用PyMuPDF以300 DPI渲染首页
doc = fitz.open(pdf_path)
page = doc[0]
pix = page.get_pixmap(matrix=fitz.Matrix(300/72, 300/72))
```

**第2步: AI图片分析**

发送到GPT-4 Vision的提示词:
```
分析这张论文首页图片,识别最重要的框架图、流程图或核心图表的位置。

要求:
1. 优先选择展示研究框架、方法流程、结果对比的图表
2. 避免纯文字区域(标题、摘要、作者信息)
3. 返回最佳裁剪区域的坐标和尺寸

返回JSON格式:
{
  "has_figure": true,
  "figure_type": "framework_diagram",
  "crop_box": {
    "x": 100,
    "y": 200,
    "width": 800,
    "height": 600
  },
  "confidence": "high",
  "description": "研究框架图,展示了三个主要模块的交互"
}
```

**第3步: 智能裁剪**
```python
# 使用LANCZOS算法高质量缩放
from PIL import Image

# 裁剪AI识别的区域
cropped = image.crop((x, y, x+width, y+height))

# 调整为目标尺寸 (400×300)
resized = cropped.resize((400, 300), Image.Resampling.LANCZOS)

# 保存为PNG
resized.save('images/papers/paper_name.png', 'PNG', quality=95)
```

**第4步: PDF复制** (供网站下载)
```python
import shutil
from pathlib import Path

# 复制PDF到下载目录
pdfs_folder = Path('pdfs')
pdfs_folder.mkdir(parents=True, exist_ok=True)
pdf_copy_path = pdfs_folder / Path(pdf_path).name

if not pdf_copy_path.exists():
    shutil.copy2(pdf_path, pdf_copy_path)
    print(f"[OK] PDF已复制至下载目录: {pdf_copy_path}")
```

**第5步: 元数据提取** (如果启用)

发送PDF前3页文本到GPT-4 Turbo:
```
分析论文文本,提取以下元数据:
- 标题 (完整标题)
- 作者 (标注†共同一作, *通讯作者)
- 期刊/会议名称
- 年份、卷号、期号、页码
- DOI

返回JSON格式...
```

**第6步: 格式化与保存**
- 格式化为APA引用
- 结构化为JSON
- 添加到 `data/publications.json`
- 生成News条目添加到 `data/news.json`

### 📊 示例输出

**封面图片**: `images/papers/cmab_2025.png`

**元数据JSON**:
```json
{
  "id": "cmab_a_multi_attribute_2025",
  "title": "CMAB: A Multi-Attribute Building Dataset of China",
  "authors": ["Zhang Y†", "Zhao H†", "Long Y*"],
  "author_note": "†co-first, *corresponding",
  "venue": "Scientific Data",
  "year": 2025,
  "volume": "12(1)",
  "pages": "430",
  "type": "journal",
  "status": "published",
  "image": "images/papers/cmab_2025.png",
  "links": {
    "pdf": "#",
    "doi": "https://doi.org/10.1038/s41597-025-04266-w"
  },
  "added_date": "2025-01-16"
}
```

**APA引用**:
```
Zhang Y†, Zhao H†, Long Y* (2025). CMAB: A Multi-Attribute Building Dataset of China. Scientific Data, 12(1), 430. https://doi.org/10.1038/s41597-025-04266-w
```

---

## 头像智能裁剪

### 🎯 功能说明

**输入**: 个人照片 (任意比例、任意尺寸)
**输出**: 400×400像素正方形头像
**处理时间**: 约10-30秒

### 📝 使用步骤

```bash
# 1. 将照片放入文件夹
cp my_photo.jpg images/raw-avatars/

# 2. 运行智能裁剪
python scripts/image_processor.py --avatar

# 3. 等待处理...
# [SEARCH] 检测到图片: my_photo.jpg
# [OK] 使用AI检测人脸位置...
# [OK] 人脸中心: (x=512, y=384)
# [OK] 智能裁剪为正方形...
# [OK] 保存到: images/profile.jpg
```

### 🔍 三重检测策略

#### 策略1: AI人脸检测 (GPT-4 Vision)

**提示词**:
```
分析这张图片,识别人脸的位置。

要求:
1. 返回人脸中心坐标
2. 返回适合的裁剪范围(确保人脸完整且居中)
3. 如果有多张人脸,选择最显著的一张

返回JSON格式:
{
  "face_detected": true,
  "face_center": {
    "x": 512,
    "y": 384
  },
  "suggested_crop": {
    "x": 312,
    "y": 184,
    "size": 400
  },
  "confidence": "high"
}
```

**优点**:
- 准确率最高
- 可处理各种角度、光照条件
- 理解上下文(如证件照、生活照)

**缺点**:
- 需要API调用
- 有费用(约$0.01/次)

#### 策略2: OpenCV本地检测 (Haar Cascade)

如果AI检测失败或未配置API,自动切换到本地检测:

```python
import cv2

# 加载预训练模型
face_cascade = cv2.CascadeClassifier(
    cv2.data.haarcascades + 'haarcascade_frontalface_default.xml'
)

# 转换为灰度图
gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)

# 检测人脸
faces = face_cascade.detectMultiScale(
    gray,
    scaleFactor=1.1,
    minNeighbors=5,
    minSize=(30, 30)
)

# 选择最大的人脸
if len(faces) > 0:
    x, y, w, h = max(faces, key=lambda f: f[2] * f[3])
    face_center = (x + w//2, y + h//2)
```

**优点**:
- 完全本地,无需网络
- 零成本
- 速度快(<1秒)

**缺点**:
- 准确率中等(约75-85%)
- 对正脸效果最好,侧脸效果差
- 受光照影响

#### 策略3: 中心裁剪 (无检测)

如果前两种方法都失败:

```python
# 计算图片中心
width, height = image.size
center_x = width // 2
center_y = height //  2

# 以中心为基准裁剪正方形
```

**优点**:
- 100%可用,永不失败
- 无需任何依赖

**缺点**:
- 可能裁剪位置不佳

### 📊 实际效果对比

| 输入图片 | AI检测 | OpenCV | 中心裁剪 |
|---------|-------|--------|----------|
| 证件照(正脸) | ✅ 完美 | ✅ 良好 | ⚠️ 可用 |
| 生活照(侧脸30°) | ✅ 完美 | ⚠️ 一般 | ❌ 可能偏移 |
| 全身照(人脸小) | ✅ 完美 | ❌ 可能失败 | ❌ 人脸太小 |
| 多人合影 | ✅ 选最显著 | ⚠️ 选最大 | ❌ 可能裁到边缘 |

**建议**:
- **正式头像**: 使用AI检测,确保完美效果
- **快速测试**: 使用OpenCV,速度快且免费
- **应急备选**: 中心裁剪,提前构图好

---

## 图片处理故障排除

### 问题1: API密钥无效

**错误信息**:
```
[ERROR] OpenAI API调用失败: Incorrect API key provided
```

**解决方法**:
1. 检查 `.env` 文件中的 `OPENAI_API_KEY` 是否正确
2. 确认密钥格式为 `sk-...`(OpenAI)或 `sk-ant-...`(Claude)
3. 检查密钥是否过期或被撤销
4. 登录OpenAI平台验证密钥状态

### 问题2: 国内网络无法访问OpenAI

**错误信息**:
```
[ERROR] OpenAI API连接超时
[ERROR] ConnectionError: [Errno 11001] getaddrinfo failed
```

**解决方法**:

**方案A: 使用中转API**
```bash
# 编辑 .env 文件
OPENAI_BASE_URL=https://api.openai-sb.com/v1
# 或其他中转服务地址
```

**方案B: 配置系统代理**
```bash
# Windows
set HTTP_PROXY=http://127.0.0.1:7890
set HTTPS_PROXY=http://127.0.0.1:7890

# Mac/Linux
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890

# 然后运行脚本
python scripts/image_processor.py --papers
```

**方案C: 使用本地检测(仅头像)**
```bash
# 安装OpenCV
pip install opencv-python

# 使用本地检测
python scripts/image_processor.py --avatar --use-opencv
```

### 问题3: PDF封面提取失败

**错误信息**:
```
[WARNING] AI识别失败,使用首页完整截图
```

**原因**:
- PDF首页没有明显的图表
- 图片分辨率太低
- AI无法识别图表类型

**解决方法**:

**方法1: 手动裁剪**
```bash
# 提取首页为图片
python scripts/pdf_cover_extractor.py --input paper.pdf --extract-only

# 使用图片编辑软件手动裁剪
# 保存为 images/papers/paper_name.png (400×300)
```

**方法2: 直接使用论文中的图表**
- 从PDF中复制图表
- 调整为400×300尺寸
- 保存到 `images/papers/`

### 问题4: 元数据提取不准确

**现象**:
- 作者顺序错误
- 期刊名称缺失
- DOI识别错误

**解决方法**:

**方法1: 手动编辑JSON**
```bash
# 脚本会显示提取结果并询问是否保存
# 选择"n"不保存
# 复制JSON模板,手动修改
# 粘贴到 data/publications.json
```

**方法2: 使用内容格式化助手**
```bash
# 交互式格式化
python scripts/content_formatter.py --type publication

# 粘贴论文信息(如Google Scholar引用、期刊官网)
# AI会格式化并让您确认
```

### 问题5: 头像裁剪位置不佳

**现象**:
- 人脸被裁剪到边缘
- 裁剪框太小/太大
- 背景过多

**解决方法**:

**方法1: 使用高质量照片**
- 分辨率至少800×800
- 人脸清晰可见
- 光线充足

**方法2: 预先构图**
- 使用照片编辑软件调整构图
- 确保人脸在图片中心
- 留出适当边距

**方法3: 调整裁剪尺寸**
```bash
# 编辑 .env 文件
AVATAR_SIZE=600  # 增大到600×600

# 重新运行
python scripts/image_processor.py --avatar
```

**方法4: 手动裁剪**
- 使用在线工具: [iloveimg.com](https://www.iloveimg.com/crop-image)
- 调整为正方形
- 保存为 `images/profile.jpg` (400×400)

### 问题6: 内存不足

**错误信息**:
```
MemoryError: Unable to allocate array
```

**原因**:
- PDF文件过大(>50MB)
- 图片分辨率过高(>5000×5000)

**解决方法**:

**方法1: 压缩PDF**
```bash
# 使用在线工具压缩
# https://www.ilovepdf.com/compress_pdf

# 或使用命令行(需安装Ghostscript)
gs -sDEVICE=pdfwrite -dCompatibilityLevel=1.4 -dPDFSETTINGS=/ebook \
   -dNOPAUSE -dQUIET -dBATCH \
   -sOutputFile=compressed.pdf original.pdf
```

**方法2: 降低渲染DPI**
```bash
# 编辑 scripts/pdf_cover_extractor.py
# 将 300 DPI 改为 150 DPI
pix = page.get_pixmap(matrix=fitz.Matrix(150/72, 150/72))
```

---

## 高级配置

### 自定义图片尺寸

**论文封面尺寸**:
```bash
# .env 文件
PAPER_COVER_WIDTH=600   # 默认400
PAPER_COVER_HEIGHT=450  # 默认300 (保持4:3比例)
```

**头像尺寸**:
```bash
# .env 文件
AVATAR_SIZE=600  # 默认400 (正方形)
```

**CSS同步修改** (`css/style.css`):
```css
.paper-thumbnail {
    width: 600px;   /* 与PAPER_COVER_WIDTH一致 */
    height: 450px;  /* 与PAPER_COVER_HEIGHT一致 */
}

.profile-photo {
    width: 600px;   /* 与AVATAR_SIZE一致 */
    height: 600px;
}
```

### 批量历史论文处理

**准备工作**:
```bash
# 1. 组织PDF文件
mkdir images/raw-papers/batch1
cp paper*.pdf images/raw-papers/batch1/

# 2. 创建元数据模板
# 如果有Google Scholar导出的BibTeX
```

**批量处理**:
```bash
# 处理所有PDF
python scripts/image_processor.py --papers

# 每篇论文会自动:
# - 提取封面
# - 提取元数据
# - 添加到publications.json
# - 生成News

# 预计时间:10篇论文 ≈ 5-10分钟
```

**后处理**:
```bash
# 检查生成的数据
cat data/publications.json | grep "title"

# 手动修正错误
code data/publications.json

# 删除重复News
code data/news.json
```

### 定期更新封面图

**场景**: 论文发表后获得更高质量的图表

**步骤**:
```bash
# 1. 删除旧封面
rm images/papers/old_paper.png

# 2. 将新PDF放入文件夹
cp updated_paper.pdf images/raw-papers/

# 3. 重新处理(使用--no-metadata避免重复添加)
python scripts/image_processor.py --papers --no-metadata

# 4. 刷新网页查看新封面
```

---

# 第二部分:内容更新管理

## 更新方式概览

### 方式1: 直接编辑JSON文件 ⭐推荐简单修改

**适用场景**:
- 修改个人信息(姓名、职位、邮箱)
- 更新统计数字
- 删除某条记录
- 修正拼写错误

**优点**:
- 快速直接
- 无需网络
- 完全可控

**缺点**:
- 需要了解JSON格式
- 容易出现语法错误
- 需手动生成News

**建议编辑器**:
- Visual Studio Code (推荐)
- Sublime Text
- Notepad++
- 在线编辑器: [jsoneditoronline.org](https://jsoneditoronline.org/)

### 方式2: 使用AI格式化助手 ⭐推荐复杂添加

**适用场景**:
- 添加新论文(从Google Scholar复制)
- 添加新数据集(从Figshare复制)
- 批量导入内容
- 格式化非结构化文本

**优点**:
- AI自动格式化
- 自动生成News
- 容错性强
- 支持自由文本输入

**缺点**:
- 需要配置OpenAI API
- 有少量费用(<$0.01/次)

---

## 配置文件详解

### config.json - 主配置文件

**位置**: `data/config.json`
**用途**: 个人信息、Biography、联系方式、研究兴趣

#### 完整结构

```json
{
  "personal": {
    "name": "Yecheng Zhang",
    "name_chinese": "张业成",
    "title": "PhD Student",
    "affiliation": "Beijing City Lab, Tsinghua University",
    "profile_image": "images/profile.jpg"
  },

  "biography": {
    "identity": "I am a PhD student at Beijing City Lab, Tsinghua University.",
    "education": "I received my bachelor's degree from Hefei University of Technology in 2023.",
    "research_interests": "My research focuses on urban modeling, urbanization processes, AI applications in urban science, and urban resilience."
  },

  "research_fields": [
    "Urban Model",
    "Urbanization",
    "AI for Urban Science",
    "Urban Resilience"
  ],

  "statistics": {
    "total_citations": {
      "display": "Total Citations",
      "value": 0,
      "auto_update": true,
      "source": "google_scholar"
    },
    "total_downloads": {
      "display": "Total Downloads",
      "value": 17000,
      "auto_update": false
    },
    "highly_cited_papers": {
      "display": "Highly Cited Papers",
      "value": 1,
      "auto_update": true,
      "source": "google_scholar"
    }
  },

  "contact": [
    {
      "display": "Email",
      "value": "zhangyec23@mails.tsinghua.edu.cn",
      "link": "mailto:zhangyec23@mails.tsinghua.edu.cn",
      "icon": "📧"
    },
    {
      "display": "Google Scholar",
      "value": "Google Scholar Profile",
      "link": "https://scholar.google.com/citations?user=YOUR_ID",
      "icon": "🎓"
    },
    {
      "display": "GitHub",
      "value": "GitHub Profile",
      "link": "https://github.com/YOUR_USERNAME",
      "icon": "💻"
    }
  ],

  "news_config": {
    "items_per_page": 10,
    "show_pagination": true,
    "auto_generate": true
  }
}
```

#### 字段说明

**personal部分**:
| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `name` | 字符串 | 英文名 | "Yecheng Zhang" |
| `name_chinese` | 字符串 | 中文名(可选) | "张业成" |
| `title` | 字符串 | 职位/身份 | "PhD Student", "Postdoc", "Assistant Professor" |
| `affiliation` | 字符串 | 所属机构 | "Beijing City Lab, Tsinghua University" |
| `profile_image` | 字符串 | 头像路径 | "images/profile.jpg" |

**biography部分** (严格3句话):
| 字段 | 说明 | 示例 |
|------|------|------|
| `identity` | 第1句:身份 | "I am a PhD student at..." |
| `education` | 第2句:教育背景 | "I received my bachelor's degree from..." |
| `research_interests` | 第3句:研究兴趣 | "My research focuses on..." |

**research_fields部分**:
- 字符串数组,每项1-5个词
- 推荐4-8个领域
- 自动显示为标签云

**statistics部分**:
| 字段 | 说明 | auto_update |
|------|------|-------------|
| `total_citations` | 总引用数 | true (从Google Scholar获取) |
| `total_downloads` | 总下载量 | false (手动维护) |
| `highly_cited_papers` | 高被引论文数 | true (从Google Scholar获取) |

**contact部分**:
- 数组,每项包含:`display`, `value`, `link`, `icon`
- 自动渲染为网格布局
- 支持任意数量的联系方式

#### 常见修改

**修改1: 更新职位**
```json
{
  "personal": {
    "title": "Postdoctoral Researcher"  // 从PhD Student改为Postdoc
  }
}
```

**修改2: 添加新联系方式**
```json
{
  "contact": [
    // ... 现有联系方式
    {
      "display": "Twitter",
      "value": "@YourHandle",
      "link": "https://twitter.com/YourHandle",
      "icon": "🐦"
    }
  ]
}
```

**修改3: 调整News分页数量**
```json
{
  "news_config": {
    "items_per_page": 15  // 从10改为15
  }
}
```

---

## 论文数据管理

### publications.json - 论文列表

**位置**: `data/publications.json`
**用途**: 存储所有论文信息

#### 完整条目示例

```json
{
  "publications": [
    {
      "id": "cmab_2025",
      "title": "CMAB: A Multi-Attribute Building Dataset of China",
      "authors": ["Zhang Y†", "Zhao H†", "Long Y*"],
      "author_note": "†co-first, *corresponding",
      "venue": "Scientific Data",
      "year": 2025,
      "volume": "12(1)",
      "pages": "430",
      "type": "journal",
      "status": "published",
      "badges": ["🏆 ESI Highly Cited Paper"],
      "image": "images/papers/cmab.png",
      "links": {
        "pdf": "https://www.nature.com/articles/xxx.pdf",
        "dataset": "https://figshare.com/xxx",
        "code": "https://github.com/xxx",
        "doi": "https://doi.org/10.1038/s41597-025-04266-w"
      },
      "citation_key": "cmab_2025",
      "added_date": "2025-01-15"
    }
  ]
}
```

#### 字段详解

**必填字段**:
| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | 字符串 | 唯一标识 | "cmab_2025" |
| `title` | 字符串 | 论文标题 | "CMAB: A Multi-Attribute Building Dataset" |
| `authors` | 数组 | 作者列表 | ["Zhang Y†", "Li M*"] |
| `venue` | 字符串 | 期刊/会议名 | "Scientific Data" |
| `year` | 数字 | 发表年份 | 2025 |
| `type` | 枚举 | 论文类型 | "journal", "conference", "preprint" |
| `status` | 枚举 | 发表状态 | "published", "accepted", "under_review" |

**可选字段**:
| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `author_note` | 字符串 | 作者注释 | "†co-first, *corresponding" |
| `volume` | 字符串 | 卷号期号 | "12(1)" |
| `pages` | 字符串 | 页码 | "430", "1-15" |
| `badges` | 数组 | 徽章标签 | ["🏆 ESI Highly Cited", "🔥 Hot Paper"] |
| `image` | 字符串 | 封面图路径 | "images/papers/cmab.png" |
| `links` | 对象 | 链接集合 | {pdf, doi, code, dataset} |
| `citation_key` | 字符串 | Google Scholar引用key | "cmab_2025" |
| `added_date` | 字符串 | 添加日期 | "2025-01-15" |

**type枚举值**:
- `journal` - 期刊论文
- `conference` - 会议论文
- `preprint` - 预印本
- `book_chapter` - 书籍章节
- `thesis` - 学位论文

**status枚举值**:
- `published` - 已发表
- `accepted` - 已接收
- `under_review` - 审稿中
- `in_press` - 印刷中

#### 添加方法

**方法1: 使用图片处理脚本自动添加**
```bash
# 将PDF论文放入文件夹
cp paper.pdf images/raw-papers/

# 运行处理(自动提取封面+元数据+添加到JSON)
python scripts/image_processor.py --papers

# 自动完成:
# - 提取封面图
# - AI识别元数据
# - 添加到publications.json
# - 生成News条目
```

**方法2: 使用AI格式化助手**
```bash
python scripts/content_formatter.py --type publication

# 粘贴论文信息(Google Scholar格式、BibTeX等)
# AI自动格式化并添加
```

**方法3: 手动添加**
```json
// 复制现有条目作为模板
{
  "id": "new_paper_2025",  // 修改ID
  "title": "New Paper Title",  // 修改标题
  "authors": ["Zhang Y", "Li M"],  // 修改作者
  "venue": "Nature",
  "year": 2025,
  "type": "journal",
  "status": "published",
  "image": "images/papers/new_paper.png",
  "links": {
    "pdf": "#",
    "doi": "https://doi.org/10.xxxx/xxxx"
  },
  "added_date": "2025-01-16"
}
```

#### 分类显示

**前端自动分类**:
- `type=journal && status=published` → "Journal Papers (Published)"
- `type=conference` → "Conference Proceedings"
- `status=under_review` → "Papers Under Review"

**自定义排序**:
- 按 `year` 降序(最新在前)
- 同年按 `added_date` 降序

#### 作者标注规范

**标注符号**:
- `†` - 共同一作 (co-first author)
- `*` - 通讯作者 (corresponding author)
- `#` - 同等贡献 (equal contribution)

**示例**:
```json
{
  "authors": ["Zhang Y†", "Li M†", "Wang K*"],
  "author_note": "†co-first, *corresponding"
}
```

---

## 数据集管理

### datasets.json - 数据集列表

**位置**: `data/datasets.json`
**用途**: 开源数据集信息

#### 完整条目示例

```json
{
  "datasets": [
    {
      "id": "glopprua",
      "name": "Global Harmonized Urban Definition (GloPPRUA)",
      "description": "A globally consistent urban dataset harmonizing multiple definitions including GHS-SMOD, GHSL-UCDB, and national census data.",
      "downloads": 5000,
      "icon": "🌍",
      "links": {
        "dataset": "https://figshare.com/articles/dataset/xxx",
        "paper": "#",
        "documentation": "https://github.com/xxx/README.md"
      },
      "related_paper": "glopprua_2024",
      "added_date": "2024-06-01",
      "version": "1.0",
      "size": "2.3 GB",
      "format": "GeoTIFF, CSV"
    }
  ]
}
```

#### 字段详解

**必填字段**:
| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | 字符串 | 唯一标识 | "glopprua" |
| `name` | 字符串 | 数据集名称 | "GloPPRUA Dataset" |
| `description` | 字符串 | 简要描述 | "A globally consistent urban dataset..." |
| `downloads` | 数字 | 下载量 | 5000 |
| `links` | 对象 | 链接集合 | {dataset, paper, documentation} |

**可选字段**:
| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `icon` | 字符串 | Emoji图标 | "🌍", "🏙️", "📊" |
| `related_paper` | 字符串 | 关联论文ID | "glopprua_2024" |
| `version` | 字符串 | 版本号 | "1.0", "2.1.3" |
| `size` | 字符串 | 数据大小 | "2.3 GB", "150 MB" |
| `format` | 字符串 | 文件格式 | "GeoTIFF, CSV", "Shapefile" |
| `license` | 字符串 | 许可证 | "CC BY 4.0", "MIT" |

#### 添加示例

```bash
# 使用AI格式化助手
python scripts/content_formatter.py --type dataset

# 输入内容(自由文本):
名称: Urban Temperature Dataset
描述: Daily temperature data for 500 global cities (2000-2023)
下载量: 1200
链接: https://figshare.com/articles/xxx
关联论文: urban_temp_2024

# AI自动格式化为JSON并添加
```

#### 自动News生成

添加数据集后自动生成News:
```
"Released Urban Temperature Dataset with 1,200 downloads."
```

---

## 奖项管理

### awards.json - 奖项列表

**位置**: `data/awards.json`
**用途**: 荣誉奖项信息

#### 完整条目示例

```json
{
  "awards": [
    {
      "year": 2025,
      "name": "\"Rising Star\" Award (Ten Young Scholars in GIS)",
      "organization": "The 13th China College GIS Forum",
      "level": "national",
      "added_date": "2025-01-10",
      "description": "Awarded to ten outstanding young scholars in GIS field across China."
    }
  ]
}
```

#### 字段详解

**必填字段**:
| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `year` | 数字 | 获奖年份 | 2025 |
| `name` | 字符串 | 奖项名称 | "Best Paper Award" |
| `organization` | 字符串 | 颁发机构 | "IEEE Conference" |

**可选字段**:
| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `level` | 枚举 | 级别 | "national", "international", "university" |
| `description` | 字符串 | 详细说明 | "Awarded to..." |
| `added_date` | 字符串 | 添加日期 | "2025-01-10" |

**level枚举值**:
- `international` - 国际级
- `national` - 国家级
- `provincial` - 省级
- `university` - 校级

#### 按年份分组显示

前端自动按年份分组:
```
📅 2025
  - "Rising Star" Award
  - Best Paper Award

📅 2024
  - National Scholarship
  ...
```

---

## 学术活动管理

### activities.json - 学术活动

**位置**: `data/activities.json`
**用途**: 展览、报告、审稿、技能

#### 完整结构

```json
{
  "activities": {
    "exhibitions": [
      {
        "title": "China Pavilion, 18th Venice Architecture Biennale",
        "year": "2023",
        "role": "Participant",
        "added_date": "2023-05-01"
      }
    ],

    "talks": [
      {
        "title": "Applied Urban Modelling (AUM2024)",
        "location": "University of Cambridge, UK",
        "year": 2024,
        "type": "oral_presentation",
        "added_date": "2024-07-15"
      }
    ],

    "peer_review": [
      {
        "journal": "Nature Cities",
        "role": "Invited Reviewer",
        "count": 3,
        "added_date": "2024-03-01"
      }
    ],

    "skills": [
      {
        "name": "Python",
        "level": "Advanced",
        "years": 5
      },
      {
        "name": "GIS (ArcGIS, QGIS)",
        "level": "Advanced",
        "years": 4
      }
    ]
  }
}
```

#### 字段详解

**exhibitions部分**:
| 字段 | 说明 | 示例 |
|------|------|------|
| `title` | 展览名称 | "Venice Architecture Biennale" |
| `year` | 年份 | "2023", "2023/2025" |
| `role` | 角色 | "Participant", "Curator" |

**talks部分**:
| 字段 | 说明 | 示例 |
|------|------|------|
| `title` | 报告主题/会议名 | "AUM2024" |
| `location` | 地点 | "University of Cambridge, UK" |
| `year` | 年份 | 2024 |
| `type` | 类型 | "oral_presentation", "poster", "keynote" |

**peer_review部分**:
| 字段 | 说明 | 示例 |
|------|------|------|
| `journal` | 期刊名 | "Nature Cities" |
| `role` | 角色 | "Invited Reviewer", "Editorial Board" |
| `count` | 审稿篇数 | 3 |

**skills部分**:
| 字段 | 说明 | 示例 |
|------|------|------|
| `name` | 技能名称 | "Python" |
| `level` | 熟练程度 | "Advanced", "Intermediate", "Basic" |
| `years` | 使用年限 | 5 |

---

## News管理

### news.json - 新闻动态

**位置**: `data/news.json`
**用途**: 所有News条目(包括置顶统计)

#### 结构说明

```json
{
  "news": [
    {
      "id": "stats_pinned",
      "date": "auto",
      "content": "📊 <strong>Total Downloads:</strong> <span id='news-downloads'>17,000+</span> | <strong>Citations:</strong> <span id='news-citations'>Loading...</span> | <strong>Highly Cited:</strong> <span id='news-highly-cited'>1</span> paper",
      "type": "statistics",
      "pinned": true,
      "auto_generated": false
    },
    {
      "id": "news_cmab_2025",
      "date": "2025-01-15",
      "content": "Our paper on <em>CMAB dataset</em> was published in <em>Scientific Data</em>.",
      "type": "publication",
      "related_id": "cmab_2025",
      "pinned": false,
      "auto_generated": true
    }
  ]
}
```

#### 字段详解

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | 字符串 | 唯一标识 | "news_001" |
| `date` | 字符串 | 日期 | "2025-01-15", "auto" |
| `content` | 字符串 | HTML内容 | "Our paper on <em>...</em>" |
| `type` | 枚举 | 类型 | "publication", "dataset", "award", "talk", "statistics" |
| `related_id` | 字符串 | 关联ID | "cmab_2025" |
| `pinned` | 布尔 | 是否置顶 | true/false |
| `auto_generated` | 布尔 | 是否自动生成 | true/false |

#### 置顶统计配置

**独特的`stats_pinned`条目**:
- `pinned: true` - 永远显示在最上方
- `date: "auto"` - 自动更新日期
- 内容中包含特殊ID(`news-downloads`, `news-citations`)
- JavaScript自动填充数值

#### 手动添加News

```json
{
  "id": "news_custom_001",
  "date": "2025-01-16",
  "content": "Invited to give a keynote speech at <em>AI4Cities 2025</em> conference.",
  "type": "talk",
  "pinned": false,
  "auto_generated": false
}
```

#### News归档

**当News过多时**(推荐>50条归档):
```bash
# 1. 备份当前News
cp data/news.json data/news_archive_2024.json

# 2. 编辑news.json,仅保留置顶统计+最近20条

# 3. 提交归档文件(可选)
git add data/news_archive_2024.json
```

---

## AI格式化助手

### content_formatter.py 使用指南

**位置**: `scripts/content_formatter.py`
**功能**: 将自由文本转换为标准JSON格式

#### 启动方式

```bash
# 添加论文
python scripts/content_formatter.py --type publication

# 添加数据集
python scripts/content_formatter.py --type dataset

# 添加奖项
python scripts/content_formatter.py --type award

# 添加学术活动
python scripts/content_formatter.py --type talk
```

#### 交互流程

**示例: 添加论文**

```bash
$ python scripts/content_formatter.py --type publication

╔══════════════════════════════════════════════════════════════════════╗
║                   AI内容格式化助手 - 论文                            ║
╚══════════════════════════════════════════════════════════════════════╝

请输入论文信息(支持以下格式):
  1. Google Scholar引用格式
  2. BibTeX格式
  3. 自由文本
  4. APA/MLA引用

输入完成后按Ctrl+D (Mac/Linux) 或 Ctrl+Z (Windows) 提交
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

张业成, 赵浩然, 龙瀛. CMAB: 中国多属性建筑数据集. Scientific Data, 2025, 12(1): 430.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[OK] 正在使用AI格式化...
[OK] 格式化完成

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
预览生成的JSON:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{
  "id": "cmab_a_multi_attribute_2025",
  "title": "CMAB: A Multi-Attribute Building Dataset of China",
  "authors": ["Zhang Y", "Zhao H", "Long Y"],
  "venue": "Scientific Data",
  "year": 2025,
  "volume": "12(1)",
  "pages": "430",
  "type": "journal",
  "status": "published",
  "image": "images/papers/cmab_a_multi_attribute_2025.png",
  "links": {
    "pdf": "#",
    "doi": "https://doi.org/10.xxxx/xxxx"
  },
  "added_date": "2025-01-16"
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

是否添加到 data/publications.json? (y/n): y

[OK] 已添加到 data/publications.json
[OK] 已生成News条目: Our paper on CMAB dataset was published in Scientific Data.
[OK] 已更新 data/news.json

完成!
```

#### 支持的输入格式

**格式1: Google Scholar引用**
```
Zhang Y, Li M, Wang K. Urban Analysis with AI. Nature Cities, 2025, 2(3): 145-160.
```

**格式2: BibTeX**
```
@article{zhang2025urban,
  title={Urban Analysis with AI},
  author={Zhang, Yecheng and Li, Ming},
  journal={Nature Cities},
  volume={2},
  number={3},
  pages={145--160},
  year={2025}
}
```

**格式3: 自由文本**
```
我们的论文"Urban Analysis with AI"发表在Nature Cities上了,
作者是Zhang Y和Li M,2025年第2卷第3期,DOI是10.xxxx/xxxx
```

**格式4: APA引用**
```
Zhang, Y., & Li, M. (2025). Urban Analysis with AI. Nature Cities, 2(3), 145-160. https://doi.org/10.xxxx/xxxx
```

#### 批量导入

**准备文本文件** (`import_papers.txt`):
```
Zhang Y, Long Y. Paper 1. Nature, 2025, 1(1): 1-10.
---
Li M, Wang K. Paper 2. Science, 2025, 2(2): 20-30.
---
...
```

**批量处理**:
```bash
python scripts/content_formatter.py --type publication --batch import_papers.txt
```

---

# 第三部分:常见场景

## 日常使用场景示例

### 场景1: 更新个人邮箱

**JSON编辑**:
```json
// data/config.json
{
  "contact": [
    {
      "display": "Email",
      "value": "newemail@university.edu",  // 修改这里
      "link": "mailto:newemail@university.edu",  // 修改这里
      "icon": "📧"
    }
  ]
}
```

**效果**: 页面Contact部分自动更新

---

### 场景2: 添加最新论文(已有PDF)

**步骤**:
```bash
# 1. 放入PDF
cp latest_paper.pdf images/raw-papers/

# 2. 自动处理
python scripts/image_processor.py --papers

# 完成!封面、元数据、News全自动生成
```

---

### 场景3: 添加论文(无PDF,有引用)

**步骤**:
```bash
# 复制Google Scholar引用
python scripts/content_formatter.py --type publication

# 粘贴引用文本,AI自动格式化
# 手动上传封面图到 images/papers/ (可选)
```

---

### 场景4: 开源新数据集

**步骤**:
```bash
python scripts/content_formatter.py --type dataset

# 输入数据集信息
名称: My Dataset
描述: A comprehensive dataset for...
下载量: 0  (初始为0)
链接: https://figshare.com/xxx
关联论文: related_paper_2025

# AI自动格式化并添加
```

**手动更新下载量**(每月更新):
```json
// data/datasets.json
{
  "datasets": [
    {
      "id": "my_dataset",
      "downloads": 1500  // 从Figshare获取最新数据
    }
  ]
}
```

---

### 场景5: 获得新奖项

**快速添加**:
```json
// data/awards.json
{
  "awards": [
    {
      "year": 2025,
      "name": "Best Paper Award",
      "organization": "ICML 2025"
    }
  ]
}
```

**生成News**:
```json
// data/news.json (手动添加)
{
  "id": "news_award_001",
  "date": "2025-01-16",
  "content": "Received <strong>Best Paper Award</strong> from ICML 2025.",
  "type": "award"
}
```

---

### 场景6: 批量导入历史论文

**准备Excel/CSV**:
```csv
title,authors,venue,year,doi
"Paper 1","Zhang Y; Li M","Nature",2024,"10.1038/xxx"
"Paper 2","Wang K; Zhang Y","Science",2023,"10.1126/yyy"
```

**转换脚本** (`convert_csv.py`):
```python
import csv
import json

with open('papers.csv', 'r', encoding='utf-8') as f:
    reader = csv.DictReader(f)
    publications = []

    for row in reader:
        pub = {
            "id": f"{row['title'][:20].lower().replace(' ', '_')}_{row['year']}",
            "title": row['title'],
            "authors": row['authors'].split('; '),
            "venue": row['venue'],
            "year": int(row['year']),
            "type": "journal",
            "status": "published",
            "links": {"doi": f"https://doi.org/{row['doi']}"}
        }
        publications.append(pub)

# 合并到现有文件
with open('data/publications.json', 'r+', encoding='utf-8') as f:
    data = json.load(f)
    data['publications'].extend(publications)
    f.seek(0)
    json.dump(data, f, ensure_ascii=False, indent=2)
```

---

## 常见问题解答

### Q1: 是否必须配置OpenAI API?

**A**: 不是必须的,但强烈推荐。

**不配置API的后果**:
- ❌ 无法使用AI识别论文封面(只能用首页截图)
- ❌ 无法自动提取元数据(需手动输入所有信息)
- ❌ 无法智能裁剪头像(依赖OpenCV或手动裁剪)

**不配置API的替代方案**:
- 论文封面:手动从PDF提取图表并裁剪
- 元数据:手动编辑 `data/publications.json`
- 头像:使用OpenCV本地检测或手动裁剪

### Q2: API调用费用大概是多少?

**A**: 非常低,大部分用户每月<$5。

**费用估算** (2025年1月定价):
- 处理1篇论文(封面+元数据): ~$0.03
- 处理1张头像: ~$0.01
- 格式化1条内容: ~$0.005

**实际使用场景**:
- 每月新增2篇论文: $0.06
- 更新1次头像: $0.01
- 添加5个数据集/奖项: $0.025

**月总计**: ~$0.10 (约合¥0.70)

**节省技巧**:
- 批量处理论文(一次性处理,避免多次调用)
- 使用 `--no-metadata` 仅提取封面
- 头像使用OpenCV本地检测

### Q3: 支持哪些文件格式?

**论文**:
- ✅ PDF (推荐)
- ❌ Word (.docx) - 需先转为PDF
- ❌ LaTeX - 需先编译为PDF

**头像**:
- ✅ JPEG (.jpg, .jpeg)
- ✅ PNG (.png)
- ✅ WEBP (.webp)
- ⚠️ HEIC (.heic) - 需先转换为JPEG

### Q4: 如何处理非英文论文?

**A**: 完全支持,AI可识别多种语言。

**中文论文**:
```bash
# 直接处理,无需额外配置
python scripts/image_processor.py --papers
```

AI会自动识别中文标题、作者、期刊名称。

**特殊字符处理**:
- 确保 `.env` 文件保存为UTF-8编码
- Python脚本已设置 `encoding='utf-8'`

### Q5: 可以处理已发表但无PDF的论文吗?

**A**: 可以,使用内容格式化助手。

```bash
# 使用格式化助手
python scripts/content_formatter.py --type publication

# 输入论文信息(Google Scholar引用、期刊官网信息等)
# AI会自动格式化为JSON

# 手动上传论文封面图到 images/papers/
```

### Q6: OpenCV人脸检测效果不好怎么办?

**A**: 尝试以下方法提升效果。

**方法1: 使用正脸照片**
- 避免侧脸、仰头、低头
- 光线充足,避免阴影
- 背景简洁

**方法2: 调整检测参数**
```python
# 编辑 scripts/ai_image_analyzer.py
faces = face_cascade.detectMultiScale(
    gray,
    scaleFactor=1.05,  # 降低(更精细,默认1.1)
    minNeighbors=3,    # 降低(更宽松,默认5)
    minSize=(50, 50)   # 增大(忽略小脸,默认30×30)
)
```

**方法3: 切换到AI检测**
```bash
# 配置OpenAI API后使用AI检测
python scripts/image_processor.py --avatar
# 不使用 --use-opencv 参数
```

### Q7: 如何处理扫描版PDF?

**A**: 扫描版PDF的文本提取效果差,建议:

**方法1: 使用OCR增强版PyMuPDF**
```bash
# 安装OCR依赖
pip install pymupdf[ocr]

# 脚本会自动使用OCR提取文字
```

**方法2: 手动输入元数据**
```bash
# 仅提取封面
python scripts/image_processor.py --papers --no-metadata

# 手动编辑publications.json添加信息
```

---

## 📚 相关文档

- [README.md](./README.md) - 项目总体说明
- [部署指南.md](./部署指南.md) - GitHub Pages部署教程
- [快速使用指南.md](./快速使用指南.md) - 基础HTML编辑指南

---

## 🙏 反馈与支持

如果遇到本指南未涵盖的问题,请:

1. 检查 [GitHub Issues](https://github.com/YOUR_USERNAME/YOUR_REPO/issues)
2. 提交新Issue并附上:
   - 错误信息完整截图
   - `.env` 配置(隐藏API密钥)
   - Python版本和依赖版本
   - 操作系统信息

---

**最后更新**: 2025-01-16
**版本**: 1.0.0
