# 🚀 个人学术主页部署指南

## 📋 目录

1. [快速预览](#快速预览)
2. [部署到 GitHub Pages](#部署到-github-pages)
3. [自定义域名](#自定义域名可选)
4. [更新网站内容](#更新网站内容)
5. [添加 Google Scholar 数据](#添加-google-scholar-数据)
6. [常见问题](#常见问题)

---

## 1. 快速预览

在部署之前，先在本地预览确保一切正常。

### 方法 A：直接打开文件

直接双击 `index.html` 文件，用浏览器打开预览。

### 方法 B：使用本地服务器（推荐）
使用本地服务器可以完整体验所有功能。

**使用 Python:**
```bash
# 在项目目录下打开命令行，运行：
python -m http.server 8000

# 然后在浏览器访问：
http://localhost:8000
```

**使用 Node.js:**
```bash
# 需要先安装 http-server
npm install -g http-server

# 运行服务器
http-server

# 访问 http://localhost:8080
```

---

## 2. 部署到 GitHub Pages

GitHub Pages 是免费的静态网站托管服务，非常适合个人主页。

### 步骤 1: 创建 GitHub 账号

如果还没有 GitHub 账号：
1. 访问 https://github.com
2. 点击 "Sign up" 注册账号
3. 验证邮箱

### 步骤 2: 创建仓库

两种方式：

#### 方式 A：个人主页（推荐）
创建名为 `用户名.github.io` 的仓库。

示例：如果你的 GitHub 用户名是 `zhangyecheng`，则创建仓库 `zhangyecheng.github.io`

**优点**：访问地址是 `https://zhangyecheng.github.io`，简洁美观

#### 方式 B：项目网站
创建任意名称的仓库，如 `my-homepage`

**访问地址**：`https://用户名.github.io/my-homepage`

### 步骤 3: 上传文件到 GitHub

#### 方法 A：使用 GitHub 网页界面（简单）

1. 进入你创建的仓库
2. 点击 "Add file" → "Upload files"
3. 将所有项目文件拖拽上传（除了 `.claude`、`.idea` 等隐藏文件夹）
4. 填写提交信息，点击 "Commit changes"

#### 方法 B：使用 Git 命令行（推荐）

```bash
# 1. 在项目文件夹打开命令行

# 2. 初始化 Git 仓库
git init

# 3. 添加所有文件
git add .

# 4. 创建第一次提交
git commit -m "Initial commit: Personal homepage"

# 5. 设置主分支名称
git branch -M main

# 6. 连接到 GitHub 仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 7. 推送到 GitHub
git push -u origin main
```

### 步骤 4: 启用 GitHub Pages

1. 进入仓库页面
2. 点击 "Settings"（设置）
3. 左侧菜单找到 "Pages"
4. 在 "Source" 下选择：
   - Branch: `main`
   - Folder: `/ (root)`
5. 点击 "Save"
6. 等待 1-2 分钟，页面会显示网站地址

### 步骤 5: 访问你的网站

- 个人主页方式：`https://你的用户名.github.io`
- 项目网站方式：`https://你的用户名.github.io/仓库名`

🎉 恭喜！你的个人主页已经上线了！

---

## 3. 自定义域名（可选）

如果你有自己的域名（如 `www.yourname.com`），可以绑定到 GitHub Pages。

### 步骤：

1. **购买域名**
   - 在阿里云、GoDaddy、Namecheap 等平台购买域名

2. **添加 DNS 记录**

   在域名管理后台添加以下记录：

   ```
   类型: A
   主机记录: @
   记录值: 185.199.108.153

   类型: A
   主机记录: @
   记录值: 185.199.109.153

   类型: A
   主机记录: @
   记录值: 185.199.110.153

   类型: A
   主机记录: @
   记录值: 185.199.111.153

   类型: CNAME
   主机记录: www
   记录值: 你的用户名.github.io
   ```

3. **在 GitHub 仓库设置自定义域名**
   - 进入仓库 Settings → Pages
   - 在 "Custom domain" 输入你的域名
   - 勾选 "Enforce HTTPS"
   - 保存

4. **等待 DNS 生效**
   - 通常需要 10 分钟到 24 小时
   - 可以用 `nslookup 你的域名` 检查

---

## 4. 更新网站内容

### 方式1：直接编辑JSON文件（推荐）

所有内容都存储在 `data/` 文件夹下的JSON文件中。

#### 修改个人信息

编辑 `data/config.json`：

```json
{
  "personal": {
    "name": "你的姓名",
    "title": "你的职位",
    "affiliation": "你的单位",
    "profile_image": "images/profile.jpg"
  },
  "biography": {
    "identity": "第一句：身份介绍",
    "education": "第二句：教育背景",
    "research_interests": "第三句：研究方向"
  },
  "contact": [
    {
      "display": "Email",
      "value": "your.email@example.com",
      "link": "mailto:your.email@example.com",
      "icon": "📧"
    },
    {
      "display": "Google Scholar",
      "value": "Google Scholar Profile",
      "link": "https://scholar.google.com/citations?user=你的ID",
      "icon": "🎓"
    },
    {
      "display": "GitHub",
      "value": "GitHub Profile",
      "link": "https://github.com/你的用户名",
      "icon": "💻"
    }
  ]
}
```

#### 添加论文

编辑 `data/publications.json`：

```json
{
  "publications": [
    {
      "id": "your_paper_2025",
      "title": "论文标题",
      "authors": ["作者1", "作者2*"],
      "author_note": "*corresponding",
      "venue": "期刊名称",
      "year": 2025,
      "volume": "卷号",
      "pages": "页码",
      "type": "journal",
      "status": "published",
      "image": "images/papers/your_paper.png",
      "links": {
        "pdf": "https://链接到PDF",
        "doi": "https://doi.org/xxx"
      }
    }
  ]
}
```

#### 添加数据集

编辑 `data/datasets.json`：

```json
{
  "datasets": [
    {
      "id": "your_dataset",
      "name": "数据集名称",
      "description": "数据集描述",
      "downloads": 下载量,
      "icon": "🌍",
      "links": {
        "dataset": "https://figshare.com/xxx",
        "paper": "#",
        "documentation": "#"
      },
      "related_paper": "论文ID"
    }
  ]
}
```

#### 添加奖项

编辑 `data/awards.json`：

```json
{
  "awards": [
    {
      "year": 2025,
      "name": "奖项名称",
      "organization": "颁发机构",
      "level": "national"
    }
  ]
}
```

### 方式2：使用批量处理脚本

#### 添加论文（自动提取元数据）

如果有论文PDF：

```bash
# 1. 将PDF放入原始文件夹
cp your_paper.pdf images/raw-papers/

# 2. 运行批量处理
python scripts/batch_processor.py

# 自动完成：
# ✅ 提取PDF完整首页为封面
# ✅ AI提取元数据（标题、作者、期刊等）
# ✅ 添加到 data/publications.json
# ✅ 生成News条目
```

#### 添加个人照片

1. 使用图片编辑工具（Photoshop/GIMP/在线工具）裁剪为400×400像素正方形
2. 命名为 `profile.jpg`
3. 放入 `images/` 文件夹

### 推送更新到GitHub

每次修改JSON文件或添加图片后：

```bash
git add .
git commit -m "Update content"
git push
```

等待1-2分钟，GitHub Pages会自动更新网站。

---

## 5. 添加 Google Scholar 数据

### 方法 1: 手动更新（简单）

编辑 `google-scholar-stats/gs_data.json`：

```json
{
    "citedby": 你的总引用数,
    "publications": {
        "cmab_2025": {
            "num_citations": 论文引用数,
            "title": "CMAB: A Multi-Attribute Building Dataset of China"
        },
        "ghost_cities_2025": {
            "num_citations": 论文引用数,
            "title": "..."
        }
    }
}
```

### 方法 2: 使用爬虫自动更新（高级）

创建 Python 脚本自动从 Google Scholar 抓取数据：

```python
# update_citations.py
from scholarly import scholarly
import json

# 你的 Google Scholar ID
author_id = '你的ID'

# 获取作者信息
author = scholarly.search_author_id(author_id)
author = scholarly.fill(author)

# 提取数据
data = {
    "citedby": author['citedby'],
    "publications": {}
}

for i, pub in enumerate(author['publications']):
    pub_id = f"paper_{i}"
    data['publications'][pub_id] = {
        "num_citations": pub['num_citations'],
        "title": pub['bib']['title']
    }

# 保存到文件
with open('google-scholar-stats/gs_data.json', 'w') as f:
    json.dump(data, f, indent=4)
```

### 方法 3: GitHub Actions 自动化（最佳）

创建 `.github/workflows/update-stats.yml`：

```yaml
name: Update Google Scholar Stats

on:
  schedule:
    - cron: '0 0 * * 0'  # 每周日更新
  workflow_dispatch:  # 手动触发

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install dependencies
        run: pip install scholarly
      - name: Update stats
        run: python update_citations.py
      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add google-scholar-stats/gs_data.json
          git commit -m "Update citation stats" || echo "No changes"
          git push
```

---

## 6. 常见问题

### Q1: 网站更新后没有变化？

**解决方案**：
1. 清除浏览器缓存（Ctrl+Shift+Delete）
2. 等待 1-2 分钟让 GitHub Pages 重新构建
3. 检查 GitHub 仓库的 Actions 标签页是否有错误

### Q2: 图片不显示？

**检查清单**：
- [ ] 图片文件是否上传到正确位置？
- [ ] 文件名大小写是否匹配？（Windows 不区分，Linux 区分）
- [ ] 图片路径是否正确？（`images/profile.jpg`）
- [ ] 图片格式是否支持？（.jpg, .png, .gif）

### Q3: 样式显示混乱？

**解决方案**：
1. 确保 `css/style.css` 文件存在
2. 检查文件编码是否为 UTF-8
3. 清除浏览器缓存

### Q4: 如何添加新论文/数据集/奖项？

**推荐方式：直接编辑JSON文件**

所有内容都存储在 `data/` 文件夹下：
- `data/publications.json` - 论文列表
- `data/datasets.json` - 数据集列表
- `data/awards.json` - 奖项列表
- `data/activities.json` - 学术活动

只需按照现有格式添加新条目，无需修改HTML/CSS/JavaScript代码。

### Q5: 如何修改颜色主题？

打开 `css/style.css`，搜索并替换颜色代码：

- `#0366d6` → 主蓝色
- `#28a745` → 绿色
- `#6f42c1` → 紫色

### Q6: 能否使用中文内容？

完全可以！只需编辑 `data/` 文件夹下的JSON文件，将英文内容替换为中文即可。

例如在 `data/config.json` 中：
```json
{
  "personal": {
    "name": "张三",
    "title": "博士研究生",
    "affiliation": "清华大学"
  }
}
```

所有JSON文件都支持UTF-8编码，可以正常显示中文。

### Q7: GitHub Pages 有访问限制吗？

GitHub Pages 的限制：
- 仓库大小 < 1 GB
- 每月带宽 < 100 GB
- 每小时构建次数 < 10 次

对个人主页来说，这些限制绰绰有余。

### Q8: 如何添加访问统计？

**方法 1: Google Analytics**

在 `index.html` 的 `<head>` 中添加：

```html
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

**方法 2: 不蒜子（简单）**

在页脚添加：

```html
<script async src="//busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js"></script>
<span id="busuanzi_container_site_pv">
    总访问量<span id="busuanzi_value_site_pv"></span>次
</span>
```

---

## 7. 进阶优化

### SEO 优化

在 `index.html` 的 `<head>` 中添加：

```html
<!-- Open Graph -->
<meta property="og:title" content="Yecheng Zhang - Personal Homepage">
<meta property="og:description" content="PhD Student at Tsinghua University">
<meta property="og:image" content="https://你的域名/images/profile.jpg">
<meta property="og:url" content="https://你的域名">

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Yecheng Zhang">
<meta name="twitter:description" content="PhD Student at Tsinghua University">
```

### 加载速度优化

1. **压缩图片**：使用 TinyPNG 或 Squoosh
2. **使用 CDN**：jQuery 和 MathJax 已使用 CDN
3. **启用缓存**：GitHub Pages 自动启用

### 添加 Favicon

创建一个小图标（16×16 或 32×32）：

```html
<link rel="icon" type="image/png" href="images/favicon.png">
```

---

## 8. 备份与版本控制

### 定期备份

GitHub 本身就是备份，但建议：

1. 本地保留源文件
2. 定期导出仓库
3. 重要修改前创建分支

### 使用分支

```bash
# 创建开发分支
git checkout -b develop

# 在开发分支修改和测试
git add .
git commit -m "Testing new feature"

# 确认无误后合并到主分支
git checkout main
git merge develop
git push
```

---

## 📞 获取帮助

- **GitHub Pages 文档**: https://pages.github.com
- **Git 教程**: https://git-scm.com/book/zh/v2
- **Markdown 指南**: https://www.markdownguide.org

---

🎉 **祝你拥有一个精美的学术主页！**

如有问题，欢迎通过 GitHub Issues 反馈。
